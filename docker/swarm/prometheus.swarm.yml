# Swarm-friendly Prometheus config (stack ubi_observe_prometheus).
# Add Docker Swarm SD or targets like ubi_core_api:3000/metrics when you expose Prometheus metrics.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
